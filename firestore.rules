rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEBUG: Allow all reads to confirm connection works
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
